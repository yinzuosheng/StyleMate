[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  ".git",
  ".pytest-tmp",
  ".superpowers",
  ".venv",
  "data/chroma",
  "data/uploads",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
